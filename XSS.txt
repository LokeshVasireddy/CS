Open Terminal / Command Prompt and run:docker pull bkimminich/juice-shop

docker run -d -p 3000:3000 bkimminich/juice-shop
http://localhost:3000
tep 1: Configure Burp Suite Proxy1. Open Burp Suite and navigate to Proxy > Intercept.2. Ensure Intercept is off.

3. Go to Proxy > Options, under Proxy Listeners, check that Burp is listening on127.0.0.1:8081.

Open your browser and configure it to use Burp Suite as a proxy:O Chrome: Settings > Proxy settings > Manual proxy > Set HTTP Proxy to127.0.0.1, Port 8081.O Firefox: Preferences > Network Settings > Manual proxy > Set HTTP Proxy to
127.0.0.1, Port 8081.

5. Verify by opening http://localhost:3000 in your browser while Burp Suite is running.Requests should appear in HTTP history.

Step 2: Identify XSS Vulnerabilities1. Open http://localhost:3000 in the browser.
2. Browse through different sections of Juice Shop and look for input fields
Open Burp Suite and navigate to Proxy > HTTP history.
4. Locate the request corresponding to an input field and send it to Repeater (Rightclick > Send to Repeater).
5. In the Repeater tab, experiment by modifying the input fields with JavaScript payloadssuch as:<script>alert("XSS')</script>
Click Send and observe the response for execution of the script.Step 3: Exploiting XSS in Feedback Form1. In Juice Shop, navigate to Contact Us or Feedback page
2. Enter the following payload in the comment field: <script>alert("XSS')</script>
3. Submit the feedback form.
4. If the site is vulnerable, an alert box will pop up.5. Confirm the XSS attack by checking:O The browser console (F12> Console).
O The HTTP history tab in Burp Suite.
Inspecting the request and response in Burp Suite.


Step 4: Stored XSS Attack1. Locate a form where the input is stored and displayed later (e.g., user profile, reviews, orfeedback section).2. Enter the payload:<script>document.write('<img src=x onerror=alert("Stored XSS")>");</script>
3. Submit the form.4. Reload the page where the stored input is displayed.



DVWA
-----
Step 1: Start DVWA Services
Open terminal:
sudo service apache2 start
sudo service mysql start
Open browser and go to:
http://localhost/dvwa

Login:
 Username: admin
 Password: password 
Click DVWA Security → set level to Low → Submit. 
Step 2: Understanding XSS
XSS allows attackers to inject JavaScript code into a webpage that runs in another user’s browser.
Types in DVWA:
 Reflected XSS
 Stored XSS
 DOM Based XSS
Step 3: Reflected XSS Test
Go to:
DVWA → XSS (Reflected) 

In the input box, type:
<script>alert('XSS')</script>
Click Submit.

Step 4: Stored XSS Test
Go to:
DVWA → XSS (Stored)
Fill the form:
Name:
<h1>Hacked</h1>
Message:
<script>alert('Stored XSS')</script>
Click Sign Guestbook. 

Step 5: DOM Based XSS
Go to:
DVWA → XSS (DOM)
In the URL bar add:
#<script>alert('DOM XSS')</script>
Press Enter → popup appears. 
Step 6: Capture Cookie (Lab Demo)
In Stored XSS Message box:
<script>alert(document.cookie)</script>
This shows session cookies (demo of session theft)


Step 7: Change Security Level
Go to DVWA Security → set:
 Medium
 High
Repeat the same payloads → see how filtering blocks them. 

